package com.tech.microservice.productenquiryservice.security;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
	/*
	 * @Bean public UserDetailsService GetBasicAurth(BCryptPasswordEncoder
	 * bCryptPasswordEncoder) { InMemoryUserDetailsManager detailsManager = new
	 * InMemoryUserDetailsManager();
	 * detailsManager.createUser(User.withUsername("user").password(
	 * bCryptPasswordEncoder.encode("userpass")).roles("").build());
	 * 
	 * return detailsManager;
	 * 
	 * }
	 */
}
